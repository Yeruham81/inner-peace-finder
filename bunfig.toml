[install]
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400

[test]
# Strips ambient OpenAI credentials so no test performs a live LLM request.
preload = ["./src/lib/test-support/offline-env.ts"]
